# Optional: PostgreSQL in production. Otherwise uses data/hulan.sqlite locally.
DATABASE_URL=
# Public URL without a trailing slash, required behind a reverse proxy.
APP_ORIGIN=
SESSION_SECRET=
ADMIN_PASSWORD=
BANK_NAME=
BANK_ACCOUNT=
BANK_HOLDER=
# Set true only on a private local preview. Never enable on a public storefront.
ALLOW_PREVIEW_ORDERS=false
